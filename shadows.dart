import 'package:flutter/material.dart';

class AppShadows {
  static const cardShadow = BoxShadow(
    color: Color(0x121d1617),
    offset: Offset(0, 10),
    blurRadius: 40,
    spreadRadius: 0,
  );

  static const blueShadow = BoxShadow(
    color: Color(0x4d95adfe),
    offset: Offset(0, 10),
    blurRadius: 22,
    spreadRadius: 0,
  );

  static const purpleShadow = BoxShadow(
    color: Color(0x4dc58bf2),
    offset: Offset(0, 10),
    blurRadius: 22,
    spreadRadius: 0,
  );

  AppShadows._();
}
